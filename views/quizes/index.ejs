<!DOCTYPE html>
<html>
  <head>
  	<meta charset="utf-8">
    <title> Preguntas </title>
    <link rel='stylesheet' type='text/css' href='/stylesheets/style.css'/>
  </head>
  <body>
	  <div class="myDivContenedor">
		<div class="myDivPregunta">
			<form method="get" action="/quizes">
				Buscar Pregunta:
				<input type="text" name="search" value="Buscar..."/>
	 			<input class="myInputLupa" type="image" src="images/lupa2.jpg" width="25" height="25">
	 		</form>
		</div>
		<div class="myPreguntas">
			<% var i; for (i=0; i < quizes.length; i++){ %>			
				<%if(session.user){%>
					<div class="myPreg">
						<div class="preg"><a href="quizes/<%= quizes[i].id %>"><%= quizes[i].pregunta %></a></div>
						<div class="edit"><a href="quizes/<%= quizes[i].id %>/edit"><button class="botonEdit"></button></a></div>
						<div class="delete">
							<form method="post" action="quizes/<%= quizes[i].id %>?_method=delete">
								<button class="botonBorrar" onClick="return confirm('Borrar: <%= quizes[i].pregunta %>');"><button>
							</form>
						</div>
					</div>							
				<%}else{%>
					<div class="myPreg">
						<a href="quizes/<%= quizes[i].id %>"><%= quizes[i].pregunta %></a>
					</div>
				<%}%>			
			<% } %>
			<div class="myPreg">
				<%if(session.user){%>
					<a href="/quizes/new"><button class="crearPregunta"></button></a>
				<%}%>
			</div>
		</div>
	</div>
	
  </body>
</html>