<!DOCTYPE html>
<html>
  <head>
  	<meta charset="utf-8">
    <title> Preguntas </title>
    <link rel='stylesheet' type='text/css' href='/stylesheets/style.css'/>
  </head>
<body>
	<h3>Página de estadísticas</h3></br>
	<table>
	<tr class="cabecera">
	  <td>Preguntas</td>
	  <td>Comentarios totales</td>
	  <td>Comentarios no publicados</td>
	  <td>Media de comentarios por pregunta</td>
	  <td>Preguntas sin comentarios</td>
	  <td>Preguntas con comentarios</td>
	</tr>
	<tr class="cuerpo">
		<td><%= statistics.quizes %></td>
		<td><%= statistics.comments %></td>
		<td><%= statistics.commentsUnpublished %>
	    (<%= statistics.comments?(statistics.commentsUnpublished * 100 / statistics.comments).toFixed(2)+'%':'100%' %>)</td>
	    <td><%= statistics.quizes?(statistics.comments / statistics.quizes).toFixed(2):0 %></td>
	    <td><%= statistics.quizes - statistics.commentedQuizes %></td>
	    <td><%= statistics.commentedQuizes %></td>
	</tr>
	</table>
</body>
</html>